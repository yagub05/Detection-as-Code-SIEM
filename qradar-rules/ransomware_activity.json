{
  "name": "QRadar - Ransomware Activity Indicators",
  "description": "Detects massive file modifications or suspicious extension changes",
  "aql_query": "SELECT sourceip, COUNT(DISTINCT filename) FROM events WHERE (payload LIKE '%.locked' OR payload LIKE '%.crypto') GROUP BY sourceip HAVING COUNT(DISTINCT filename) > 100 TIMESPAN 2m",
  "severity": 9,
  "type": "Custom Rule"
}
