{
  "name": "QRadar - Ransomware Activity Indicators",
  "description": "Detects massive file modifications or suspicious extension changes",
  "aql_query": "SELECT sourceip, COUNT(DISTINCT filename) FROM events WHERE (UTF8(payload) LIKE '%.locked' OR UTF8(payload) LIKE '%.crypto') GROUP BY sourceip HAVING COUNT(DISTINCT filename) > 100 LAST 2 MINUTES",
  "severity": 9,
  "type": "Custom Rule"
}
