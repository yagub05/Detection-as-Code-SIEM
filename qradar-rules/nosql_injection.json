{
  "name": "QRadar - NoSQL Injection Attempt",
  "description": "Detects MongoDB/NoSQL injection operators in application payloads",
  "aql_query": "SELECT sourceip, url FROM events WHERE (payload LIKE '%$gt%' OR payload LIKE '%$ne%' OR payload LIKE '%$where%')",
  "severity": 7,
  "type": "Custom Rule"
}
