{
  "name": "QRadar - SSRF Attack Attempt",
  "description": "Detects Server-Side Request Forgery targeting cloud metadata or internal network",
  "aql_query": "SELECT sourceip, url FROM events WHERE (url LIKE '%169.254.169.254%' OR url LIKE '%localhost%')",
  "severity": 8,
  "type": "Custom Rule"
}
